const weights = [20, 50, 40, 70, 10, 30, 80];
const amount = weights.length;
const container = 0;

const OneDPacking = (weights: number[], amount: number, container: number) => {
  const result = 0;

  const bin_remaining = Array(amount).fill(0);

  for (let i = 0; i < amount; i++) {
    let j;

    let minimumSpace = container + 1,
      bi = 0;
  }
};
